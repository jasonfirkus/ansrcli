import type Question from "../../types/question.js";
import { Box } from "ink";
import React from "react";

const MultipleChoiceQuestion = ({ data }: { data: Question }) => {
  return <Box></Box>;
};

export default MultipleChoiceQuestion;
