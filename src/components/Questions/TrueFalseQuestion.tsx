import type Question from "../../types/question.js";
import { Box } from "ink";
import React from "react";

const TrueFalseQuestion = ({ data }: { data: Question }) => {
  return <Box></Box>;
};

export default TrueFalseQuestion;
