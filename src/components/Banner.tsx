import React from "react";
import Gradient from "ink-gradient";
import BigText from "ink-big-text";

const Banner = () => {
  return (
    <Gradient name="vice">
      <BigText text="ansrcli" font="block" letterSpacing={3} />
    </Gradient>
  );
};

export default Banner;
